import React, { memo } from 'react';

import type { IconBaseProps } from 'react-icons';
import { RiAdminFill } from 'react-icons/ri';

export const AdminIcon = memo((props: IconBaseProps) => <RiAdminFill {...props} />);
