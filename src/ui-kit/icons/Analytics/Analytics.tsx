import React, { memo } from 'react';

import type { IconBaseProps } from 'react-icons';
import { SiGoogleanalytics } from 'react-icons/si';

export const AnalyticsIcon = memo((props: IconBaseProps) => <SiGoogleanalytics {...props} />);
